# AI伙伴记忆系统设计文档

## 1. 系统概述

本设计文档描述了一个具有三层记忆结构的AI伙伴系统，旨在通过向量数据库实现接近真人的长期记忆功能。系统采用混合存储方案，结合了内存存储和向量数据库，以实现高效的记忆管理和检索。

## 2. 记忆架构设计

### 2.1 三层记忆结构

#### 短期记忆（工作记忆）
- **存储位置**：内存，队列存储
- **内容**：最近几轮对话，完整的对话历史
- **特点**：快速访问，完整保留对话上下文
- **容量限制**：配置最大对话轮数（默认12轮）

#### 中期记忆
- **存储位置**：ChromaDB向量数据库
- **内容**：近期重要对话的精炼摘要
- **特点**：语义检索，基于话题组织
- **生命周期**：约一周时间，之后可能转化为长期记忆

#### 长期记忆
- **存储位置**：ChromaDB向量数据库
- **内容**：持久化的重要信息、用户偏好、历史事件等
- **特点**：高度抽象的知识，稳定的用户特征
- **生命周期**：长期保存，定期更新

## 3. 记忆流转机制

### 3.1 短期记忆到中期记忆的流转

#### 触发条件
- 当短期记忆达到配置的最大对话轮数（默认20轮）
- 系统自动触发记忆流转过程

#### 流转流程
1. **话题分割**：
   - 使用LangChain Agent自主分析完整对话历史
   - Agent通过调用工具将对话分割为多个完整的话题单元
   - 确保每个话题单元包含完整的讨论，不切断正在进行的对话

2. **记忆卡片生成**：
   - Agent为除最后一个话题(原因是话题可能未结束)外的所有话题生成中期记忆卡片
   - 每个记忆卡片包含话题摘要、关键信息点、情感色彩等
   - 计算每个记忆卡片的重要性评分
   - 某条对话若完全无记忆价值，则直接不参与记忆卡片的生成

3. **短期记忆更新**：
   - 保留最后一个话题的对话在短期记忆中
   - 将保留的对话移动到队列首部
   - 删除已被总结为中期记忆的对话


### 3.2 中期记忆到长期记忆的流转

#### 触发条件
- 定时任务（默认每周执行一次）
- 分析全部的中期记忆

#### 流转流程
1. **记忆重整**：
   - 使用LangChain Agent自主分析全部的中期记忆
   - Agent通过调用工具合并内容类似的记忆卡片（删除原来的两个，创建一个新的，不存在"更新"）

2. **长期记忆生成**：
   - Agent将重要性高，需要长久记忆的中期记忆精炼为长期记忆卡片
   - 提取持久化的知识或模式
   - 识别用户特征或偏好

3. **记忆清理**：
   - Agent清理重要程度低，创建时间久的中期记忆卡片
   - 高重要性的记忆保留更长时间


## 4. 记忆检索机制

### 4.1 LLM调用时的记忆检索

#### 检索流程
1. **短期记忆获取**：
   - 直接获取完整的最近对话历史列表
   - 完整包含在用户提示中

2. **相关记忆检索**：
   - 基于当前查询和最近对话的语义
   - 从中期记忆检索相关记忆卡片（默认3个）
   - 从长期记忆检索相关记忆卡片（默认2个）

3. **上下文构建**：
   - 系统提示
   - 最近对话（完整的短期记忆队列）
   - 长期记忆（最基础的信息）
   - 中期记忆（近期相关对话）
   - 当前时间
   - 当前用户发送消息内容

## 5. 记忆卡片结构

### 5.0 短期记忆队列
- 消息时间
- 消息发送方
- 消息内容

### 5.1 中期记忆卡片
- 话题摘要    
- 关键信息点
- 话题标签
- 重要性评分
- 创建时间
- 情感色彩
- 对话类型

### 5.2 长期记忆卡片
- 主题
- 抽象化知识摘要
- 用户模式或偏好
- 重要性评分
- 创建时间和更新时间
- 源记忆关联
- 置信度评分
- 记忆类别

## 6. 记忆重要性评估

### 6.1 评估因子
- 情感强度：对话中表达的情感强度
- 信息密度：对话中包含的信息量
- 用户参与度：用户的参与程度
- 知识重要性：话题包含的内容中是否有表达了用户喜好，需要长期遵守的规则等知识
- 决策重要性：是否包含重要决策

### 6.2 评分机制
- 多因子加权计算
- 动态调整权重
- 确保评分在0-1范围内

## 7. 系统动态配置参数
- apikey配置
- 模型名称配置

### 7.1 短期记忆配置
- 最大对话轮数

### 7.1 对话配置
- 系统提示文本文件
- 用户提示文本文件（包含最近对话，长期记忆，中期记忆，当前时间，当前用户发送消息内容的占位符，发送时填充）

### 7.4 长期记忆生成配置
- 执行中期记忆归档，重整的间隔

## 8. LangChain Agent技术实现

### 8.1 Agent架构设计

#### 核心概念
- **Agent（代理）**：具有推理能力的LLM，可以自主决定调用哪些工具
- **Tools（工具）**：Agent可以调用的函数，每个工具都有明确的描述
- **Task（任务）**：给Agent的具体目标和约束条件

#### 短期记忆归档Agent
- **任务目标**：将短期记忆归档为中期记忆
- **工具集**：获取短期记忆、话题分割、创建记忆卡片、存储记忆、更新短期记忆
- **处理规则**：
  - 确保每个话题单元包含完整的讨论，不切断正在进行的对话
  - 每个话题单元至少包含2轮对话
  - 保留最后一个话题的对话在短期记忆中，确保对话连续性
  - 为每个话题创建简洁准确的摘要
  - 识别话题的情感色彩和重要性

#### 中期记忆重整Agent
- **任务目标**：将中期记忆重整，并生成长期记忆
- **工具集**：获取中期记忆、记忆分组、创建长期记忆、存储长期记忆、归档旧记忆
- **处理规则**：
  - 识别相关的记忆，将它们组织在一起
  - 提取持久化的知识或模式
  - 识别用户特征或偏好
  - 确保长期记忆的准确性和重要性
  - 只保留高重要性的中期记忆


## 9. 日志记录

### 9.1 info级别
- 系统启动
- 短期记忆归档
- 中期记忆重整

### 9.2 debug级别
- 用户发送的消息
- LLM调用时的上下文（系统提示，用户提示）
- llm返回的消息